# 2026-08-06

## P34 学习笔记按"好版本标准"重写
- 对比了 P34 旧版笔记（5.5KB，表格速查卡式，6/17 生成）与 P33 新版笔记（16KB，教材级，6/22 生成）
- 提取出 12 条笔记转换要求（教材级教学笔记：密度 60-80%、遵循讲解顺序、保留类比、Callout 块、SVG 图、ASR 纠错、知识总结口诀、落款）
- 按此要求重写 P34（硬件 I2C 外设，纯理论课）：
  - 覆盖原文件 `P34-[10-4] I2C通信外设/..._学习笔记.md`（5.5KB → 15KB）
  - 13 章节结构，含软件vs硬件对比、多主机模型、7/10位地址、引脚映射、外设框图、主机收发流程（EV5/EV6/EV8/EV8_2、EV7/EV7_1）、波形对比、手册导读、知识总结
  - 新增 `diagrams/` 目录，7 张 SVG 矢量图（外设框图、引脚映射、7位vs10位地址、多主机模型、主机发送流程、主机接收流程、知识总结）
- 质量检查：15138 字符（远超 8000 目标）、7 处 SVG 引用全部有效、无 ASR 误识别残留、无 AI 自检模板痕迹
- 经验：ASR 转录文本质量差（"转件"=软件、"金融器"=寄存器等），转述时必须全部校正；密度衡量以"教材级详细度"为准而非死抠百分比

## 监督智能体团队并行重写 P36-P49（14 篇笔记）
- 用户要求监督模式：每个智能体只处理一篇，可并行，全部完成才退出
- 架构：TeamCreate 团队 + 分批拉起 general-purpose 智能体（每批 4 个，第 4 批 2 个）+ 每篇完成即验证（字符数≥15K/无ASR残留/SVG引用有效/无旧模板）+ 不合格 SendMessage 返工
- 成果：14/14 全部达标，每篇 17-26K 字符、3-5 张 SVG 图、章节式 + Callout + 落款
- 关键经验：
  1. 智能体可能报告"完成"但未真正写入（P36 踩坑）→ prompt 必须要求"用 Write 覆盖写入"并说明当前文件是旧版，且完成后必须验证
  2. 会话/团队上下文可能丢失导致任务中断（P46 丢失）→ 恢复团队后重新拉起即可，文件状态为准
  3. 超长单行 transcript.txt 需用 Python 分段读取（Read 会截断）→ 已写进所有 prompt
  4. SVG 文件名必须小写（CamelCase 会造成笔记引用不匹配，产生冗余文件）
  5. 中文路径下 rm 的安全删除会失败 → 冗余文件保留或改期处理
